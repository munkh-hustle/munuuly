name: munuuly
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  url_launcher: ^6.1.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1  # Add this line

flutter:
  uses-material-design: true
  
  assets:
    - assets/1.jpg
flutter_launcher_icons:
  android: "launcher_icon"
  image_path: "assets/1.jpg"
  remove_alpha_android: true
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/1.jpg"